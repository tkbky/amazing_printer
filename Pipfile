[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = ">=4.3.0"
pytest-cov = ">=2.6.0"
tox = ">=3.7.0"
codecov = "*"

[packages]
colorama = ">=0.4.0"
pyyaml = ">=5.1.1"

[requires]
python_version = "3.7"
